# Server-side only. Do NOT prefix this with NEXT_PUBLIC_ — that would bundle
# it into client-side JS and expose it to anyone who views page source.
# Only app/api/* route handlers (which run on the server) read this.

NVIDIA_API_KEY_MINIMAX=
